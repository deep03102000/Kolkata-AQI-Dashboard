# Build-Time Scripts

This folder is reserved for build-time-only geometry and data tooling.

Keep scripts here instead of `src/` so it stays clear that these files are not part of the runtime app bundle.

Planned use:

- station distance calculations from the Phase 1.11 station master table
- Voronoi coverage generation
- other preprocessing utilities
